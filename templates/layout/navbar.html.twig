{% set route = app.request.get('_route') %}

<div class="navbar-wrapper">
    <div class="nav-container container">
        <a class="nav-logo" href="{{ path('home') }}">
            <img src="{{ asset('assets/images/cyannlab-Q.png') }}" alt="cyannlab">
        </a>
        <nav class="nav-top">
            <ul>
                <li class="nav-item"><a class="nav-link {{ 'works' in route ? 'active' }}" href="{{ path('works') }}">Réalisations</a></li>
                <li class="nav-item"><a class="nav-link {{ route == 'contact' ? 'active' }} " href="{{ path('contact') }}">contact</a></li>
                {% if app.user %}
                    <li class="nav-item"><a class="nav-link {{ route == 'admin' ? 'active' }} " href="{{ path('admin') }}">admin</a></li>
                {% endif %}
            </ul>
        </nav>
        <div class="burger-top-nav">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>
</div>