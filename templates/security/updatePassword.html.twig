{% extends 'base.html.twig' %}

{% block stylesheets %}
     {{ parent() }}
    {{ encore_entry_link_tags('connexion') }}
{% endblock %}


{% block body %}
<div class="container-full container-login bg-light" >
    <section class="contact-section">
        <form method="post" class="contact-form login-form">

            {% for message in app.flashes('notice') %}
                <div class="alert alert-info">{{ message }}</div>
            {% endfor %}
            
            <h1 class="form-title">{{pageTitle}}</h1>
            <br/>
            
            {{ form_start(form) }}            
                <div class="form-group">
                    {{ form_label(form.newPassword.first) }}
                    {{ form_widget(form.newPassword.first) }}
                    <div class="form-error-message">{{ form_errors(form.newPassword.first) }}</div>
                </div>
                <div class="form-group">
                    {{ form_label(form.newPassword.second) }}
                    {{ form_widget(form.newPassword.second) }}
                    <div class="form-error-message">{{ form_errors(form.newPassword.second) }}</div>
                </div>                

                {{ form_rest(form) }}

                <div class="flex-wrap jc-end">
                    <button class="btn btn-submit" type="submit">Mettre à jour</button>
                </div>
            {{ form_end(form) }}
        </form>
    </section>
</div>
{% endblock %}
